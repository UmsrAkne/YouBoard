<UserControl
    x:Class="YouBoard.Views.IssueListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:YouBoard.Views.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:YouBoard.Models"
    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
    xmlns:viewModels="clr-namespace:YouBoard.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:DesignIssueListViewModel}"
    d:DesignHeight="500"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ObjectDataProvider
            x:Key="IssueTypeEnum"
            MethodName="GetValues"
            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:IssueType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <!-- Converters -->
        <converters:IssueDatesAndDurationsConverter x:Key="IssueDatesAndDurationsConverter" />
    </UserControl.Resources>

    <UserControl.InputBindings>
        <KeyBinding
            Key="Enter"
            Command="{Binding CreateIssueCommand}"
            Modifiers="Control+Shift" />
    </UserControl.InputBindings>

    <Grid Background="{StaticResource DarkBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <DockPanel Margin="4">
                <StackPanel Orientation="Horizontal">
                    <Button
                        Height="auto"
                        Padding="20,3"
                        Command="{Binding CopyIssueCommand}">
                        <Button.Content>
                            <materialDesign:PackIcon
                                Width="30"
                                Height="15"
                                Kind="ContentCopy" />
                        </Button.Content>
                    </Button>

                    <Button
                        Height="auto"
                        Margin="10,0"
                        Padding="20,3"
                        Command="{Binding GenerateNextIssueCopyCommand}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    Width="20"
                                    Height="20"
                                    Kind="Plus" />

                                <materialDesign:PackIcon
                                    Width="30"
                                    Height="20"
                                    Kind="Numeric" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>


                <Button
                    Width="80"
                    Margin="10,0"
                    HorizontalAlignment="Right"
                    Command="{Binding ShowBulkCreateIssuePageCommand}"
                    DockPanel.Dock="Right">

                    <Button.Content>
                        <materialDesign:PackIcon
                            Width="30"
                            Height="25"
                            Kind="PlaylistAdd" />
                    </Button.Content>
                </Button>

                <Border
                    Padding="5,0"
                    HorizontalAlignment="Right"
                    BorderBrush="Aqua"
                    BorderThickness="1"
                    CornerRadius="4"
                    DockPanel.Dock="Right">

                    <materialDesign:PopupBox
                        Margin="5"
                        VerticalAlignment="Top"
                        PlacementMode="BottomAndAlignCentres"
                        StaysOpen="True">

                        <materialDesign:PopupBox.ToggleContent>
                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    Foreground="Aqua"
                                    Kind="SortVariant" />
                                <TextBlock
                                    Margin="4,0,0,0"
                                    FontSize="18"
                                    Foreground="Aqua"
                                    Text=" Sort" />
                            </StackPanel>
                        </materialDesign:PopupBox.ToggleContent>

                        <materialDesign:PopupBox.PopupContent>
                            <StackPanel Margin="5">
                                <StackPanel.Resources>
                                    <Style BasedOn="{StaticResource {x:Type RadioButton}}" TargetType="RadioButton">
                                        <Setter Property="Command" Value="{Binding ReloadIssuesCommand}" />
                                        <Setter Property="GroupName" Value="SortOrder" />
                                        <Setter Property="Margin" Value="0,3" />
                                    </Style>
                                </StackPanel.Resources>
                                <RadioButton Content="作成日" IsChecked="{Binding IssueSearchOption.IsSortByCreatedDate}" />
                                <RadioButton Content="課題タイトル" IsChecked="{Binding IssueSearchOption.IsSortByTitle}" />
                                <RadioButton Content="課題タイプ" IsChecked="{Binding IssueSearchOption.IsSortByType}" />
                                <CheckBox
                                    Margin="0,10,0,0"
                                    Content="昇順"
                                    IsChecked="{Binding IssueSearchOption.IsAscending}" />
                            </StackPanel>
                        </materialDesign:PopupBox.PopupContent>
                    </materialDesign:PopupBox>
                </Border>

                <Button
                    Margin="10,0"
                    Padding="5,0"
                    HorizontalAlignment="Right"
                    BorderThickness="1"
                    Command="{Binding ToggleResolvedFilterCommand}"
                    DockPanel.Dock="Right">

                    <Button.Style>
                        <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IssueSearchOption.IsOnlyUnresolved}" Value="True">
                                    <Setter Property="Background" Value="LightSalmon" />
                                    <Setter Property="BorderThickness" Value="1" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IssueSearchOption.IsOnlyUnresolved}" Value="False" />
                            </Style.Triggers>
                        </Style>
                    </Button.Style>

                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <materialDesign:PackIcon
                            Width="20"
                            Height="20"
                            VerticalAlignment="Center"
                            Kind="CheckboxBlankBadgeOutline" />
                        <TextBlock
                            Margin="4,0,0,0"
                            FontSize="16"
                            Text=" Unresolved" />
                    </StackPanel>
                </Button>

            </DockPanel>
        </Border>

        <ListBox
            Grid.Row="1"
            HorizontalContentAlignment="Stretch"
            AlternationCount="2"
            ItemsSource="{Binding IssueWrappers}"
            ScrollViewer.CanContentScroll="True"
            SelectedItem="{Binding SelectedItem}"
            VirtualizingPanel.ScrollUnit="Pixel">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:IssueWrapper}">
                    <Grid Tag="{Binding DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBox}}}">
                        <Grid.ContextMenu>
                            <ContextMenu>

                                <!-- ReSharper disable Xaml.BindingWithContextNotResolved -->
                                <MenuItem
                                    Command="{Binding PlacementTarget.(FrameworkElement.Tag).ToObsoleteCommandAsync, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}"
                                    CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                    Header="Obsolete" />
                                <!-- ReSharper restore Xaml.BindingWithContextNotResolved -->

                            </ContextMenu>
                        </Grid.ContextMenu>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="PreviewMouseDown">
                                <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.ApplySelectionCommand}" CommandParameter="{Binding}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Offset="0" Color="LightBlue" />
                                                        <GradientStop Offset="1" Color="LightSkyBlue" />
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Path=IsSelected}" Value="False">
                                            <Setter Property="Background" Value="Transparent" />
                                        </DataTrigger>

                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>

                        <Expander
                            Grid.Column="1"
                            Background="Transparent"
                            Foreground="{StaticResource LightFgBrush}"
                            IsExpanded="{Binding IsExpanded}"
                            Style="{StaticResource CustomExpander}">
                            <i:Interaction.Triggers>

                                <i:EventTrigger EventName="Expanded">
                                    <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.LoadCommentsCommandAsync}" CommandParameter="{Binding}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <Expander.Header>
                                <Grid>
                                    <Grid.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource LightFgBrush}" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="FontSize" Value="{StaticResource GlobalFontSize}" />
                                        </Style>
                                    </Grid.Resources>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal">

                                        <CheckBox
                                            Width="20"
                                            Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.AddWorkingDurationCommandAsync}"
                                            CommandParameter="{Binding}"
                                            Foreground="Aqua"
                                            IsChecked="{Binding IsComplete}"
                                            IsEnabled="{Binding IsComplete, Converter={StaticResource InverseBooleanConverter}}">
                                            <CheckBox.LayoutTransform>
                                                <ScaleTransform ScaleX="1.2" ScaleY="1.2" />
                                            </CheckBox.LayoutTransform>

                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="Checked">
                                                    <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.MarkAsCompleteIssueCommand}" CommandParameter="{Binding}" />
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>

                                        </CheckBox>
                                        <Button
                                            Width="120"
                                            Height="25"
                                            Margin="8,0"
                                            Padding="5,0"
                                            HorizontalContentAlignment="Left"
                                            Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.ToggleIssueStateCommandAsync}"
                                            CommandParameter="{Binding}">

                                            <Button.Style>
                                                <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding WorkTimer.IsRunning}" Value="True">
                                                            <Setter Property="Content">
                                                                <Setter.Value>
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="30" />
                                                                            <ColumnDefinition />
                                                                        </Grid.ColumnDefinitions>

                                                                        <materialDesign:PackIcon
                                                                            Grid.Column="0"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"
                                                                            Kind="Pause" />

                                                                        <TextBlock
                                                                            Grid.Column="1"
                                                                            HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"
                                                                            Foreground="Black"
                                                                            Text="{Binding WorkTimer.Elapsed, Converter={StaticResource TimeSpanToStringConverter}}" />
                                                                    </Grid>
                                                                </Setter.Value>
                                                            </Setter>

                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                        <GradientStop Offset="0" Color="DarkOrange" />
                                                                        <GradientStop Offset="1" Color="Orange" />
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>

                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard x:Name="BeginStoryboard">
                                                                    <Storyboard AutoReverse="True" RepeatBehavior="Forever">
                                                                        <ColorAnimation
                                                                            Storyboard.TargetProperty="(Button.Background).(LinearGradientBrush.GradientStops)[0].(GradientStop.Color)"
                                                                            To="Coral"
                                                                            Duration="0:0:2" />
                                                                        <ColorAnimation
                                                                            Storyboard.TargetProperty="(Button.Background).(LinearGradientBrush.GradientStops)[1].(GradientStop.Color)"
                                                                            To="LightCoral"
                                                                            Duration="0:0:2" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>

                                                            <DataTrigger.ExitActions>
                                                                <StopStoryboard BeginStoryboardName="BeginStoryboard" />
                                                            </DataTrigger.ExitActions>

                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding WorkTimer.IsRunning}" Value="False">
                                                            <Setter Property="Content">
                                                                <Setter.Value>
                                                                    <materialDesign:PackIcon
                                                                        Width="30"
                                                                        HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"
                                                                        Kind="PlayArrow" />
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush x:Name="MyGradient" StartPoint="0,0" EndPoint="1,0">
                                                                        <GradientStop x:Name="StartStop" Offset="0" Color="DeepSkyBlue" />
                                                                        <GradientStop x:Name="EndStop" Offset="1" Color="DodgerBlue" />
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding IsComplete}" Value="True">
                                                            <Setter Property="IsEnabled" Value="False" />
                                                            <Setter Property="Content">
                                                                <Setter.Value>
                                                                    <materialDesign:PackIcon
                                                                        Width="30"
                                                                        HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"
                                                                        Kind="CheckBold" />
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding State}">
                                                            <DataTrigger.Value>
                                                                <models:IssueState>Obsolete</models:IssueState>
                                                            </DataTrigger.Value>

                                                            <Setter Property="IsEnabled" Value="False" />
                                                            <Setter Property="Content">
                                                                <Setter.Value>
                                                                    <materialDesign:PackIcon
                                                                        Width="30"
                                                                        HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"
                                                                        Kind="Cancel" />
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Border
                                            Width="80"
                                            BorderBrush="DarkBlue"
                                            BorderThickness="1"
                                            CornerRadius="12,4,4,12">

                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsComplete}" Value="True">
                                                            <Setter Property="Background" Value="#3333aa" />
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding IsComplete}" Value="False">
                                                            <Setter Property="Background" Value="#330099" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <TextBlock HorizontalAlignment="Center" Text="{Binding State}">

                                                <TextBlock.Style>
                                                    <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsComplete}" Value="True">
                                                                <Setter Property="Foreground" Value="LightSlateGray" />
                                                            </DataTrigger>

                                                            <DataTrigger Binding="{Binding IsComplete}" Value="False">
                                                                <Setter Property="Foreground" Value="White" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>

                                    </StackPanel>

                                    <TextBlock
                                        Grid.Column="1"
                                        Margin="8,0"
                                        Padding="0,0,0,2"
                                        Text="{Binding Title}">
                                        <TextBlock.Style>
                                            <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsComplete}" Value="True">
                                                        <Setter Property="Foreground" Value="LightSlateGray" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding IsComplete}" Value="False">
                                                        <Setter Property="Foreground" Value="White" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <TextBlock
                                        Grid.Column="3"
                                        Width="50"
                                        Margin="3,0"
                                        Text="{Binding Type, StringFormat=({0}), Converter={StaticResource IssueTypeToStringConverter}}" />

                                    <TextBlock
                                        Grid.Column="4"
                                        MinWidth="40"
                                        Text="{Binding Id}" />
                                </Grid>
                            </Expander.Header>

                            <Expander.Content>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>

                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>

                                    <StackPanel
                                        Grid.Row="0"
                                        Grid.Column="2"
                                        Margin="10,4"
                                        HorizontalAlignment="Right"
                                        Orientation="Horizontal">

                                        <TextBlock HorizontalAlignment="Right" FontSize="{StaticResource GlobalFontSize}">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource IssueDatesAndDurationsConverter}">
                                                    <Binding Path="Created" />
                                                    <Binding Path="ElapsedDuration" />
                                                    <Binding Path="EstimatedDuration" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>

                                    </StackPanel>

                                    <Border
                                        Grid.Row="1"
                                        Grid.Column="0"
                                        Width="65" />

                                    <Button
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Height="20"
                                        Margin="0,4"
                                        Padding="15,1"
                                        VerticalAlignment="Center"
                                        Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.AddCommentCommandAsync}"
                                        CommandParameter="{Binding}">
                                        <Button.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="CommentPlus" />
                                                <Border Margin="4,0" />
                                                <TextBlock VerticalAlignment="Center" Text="(ctr + Enter)" />
                                            </StackPanel>
                                        </Button.Content>
                                    </Button>

                                    <TextBox
                                        Grid.Row="1"
                                        Grid.Column="2"
                                        Margin="10,0"
                                        VerticalAlignment="Center"
                                        Text="{Binding PendingComment, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBox.InputBindings>
                                            <KeyBinding
                                                Key="Enter"
                                                Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.AddCommentCommandAsync}"
                                                CommandParameter="{Binding}"
                                                Modifiers="Control" />
                                        </TextBox.InputBindings>
                                    </TextBox>

                                    <ListBox
                                        Grid.Row="2"
                                        Grid.Column="2"
                                        Margin="3,5"
                                        HorizontalContentAlignment="Stretch"
                                        ItemsSource="{Binding Comments}"
                                        PreviewMouseWheel="InnerListBox_PreviewMouseWheel">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:IssueCommentWrapper}">
                                                <DataTemplate.Resources>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="FontSize" Value="{StaticResource GlobalFontSize}" />
                                                    </Style>
                                                </DataTemplate.Resources>

                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock
                                                        Grid.Column="0"
                                                        Foreground="{StaticResource LightFgBrush}"
                                                        Text="{Binding Text}" />
                                                    <TextBlock
                                                        Grid.Column="1"
                                                        HorizontalAlignment="Right"
                                                        Foreground="{StaticResource LightFgBrush}"
                                                        Text="{Binding CreatedAt, StringFormat=yyyy/MM/dd HH:mm:ss}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                            </Expander.Content>
                        </Expander>
                    </Grid>

                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemContainerStyle>
                <Style BasedOn="{StaticResource MaterialDesignListBoxItem}" TargetType="ListBoxItem">
                    <Setter Property="Opacity" Value="0" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                    <Style.Triggers>

                        <!-- デザイン時は Opacity = 1 -->
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.IsDesignInstance}" Value="True">
                            <Setter Property="Opacity" Value="1" />
                        </DataTrigger>

                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter Property="Background" Value="{StaticResource DarkBgBrush}" />
                        </Trigger>

                        <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                            <Setter Property="Background" Value="{StaticResource MediumBgBrush}" />
                        </Trigger>

                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Opacity"
                                        From="0"
                                        To="1"
                                        Duration="0:0:0.3" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="2" Margin="5">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="WhiteSmoke" />
                </Style>
                <Style BasedOn="{StaticResource MaterialDesignTextBox}" TargetType="TextBox">
                    <Setter Property="Foreground" Value="WhiteSmoke" />
                </Style>
            </StackPanel.Resources>
            <DockPanel>
                <TextBlock Width="80" Text="Title :" />
                <TextBox Margin="3,0" Text="{Binding PendingIssue.Title, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>

            <Border Margin="0,2" />

            <DockPanel>
                <TextBlock Width="80" Text="Description :" />
                <TextBox Margin="3,0" Text="{Binding PendingIssue.Description, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>

            <Border Margin="0,2" />

            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Border>
                    <materialDesign:PackIcon
                        Height="25"
                        VerticalAlignment="Center"
                        Foreground="White"
                        Kind="HourglassComplete" />
                </Border>
                <TextBlock
                    Margin="5,0"
                    VerticalAlignment="Center"
                    Text="Est. Duration" />
                <TextBox
                    Width="60"
                    Text="{Binding PendingIssue.EstimatedDuration, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource TimeSpanToStringConverter}, ConverterParameter=TotalMinutes}"
                    TextAlignment="Center" />

                <TextBlock
                    Margin="3,0,10,0"
                    VerticalAlignment="Center"
                    Text="min" />

                <TextBlock VerticalAlignment="Center" Text="Type :" />
                <ComboBox
                    Width="120"
                    Height="30"
                    Margin="8,0"
                    Padding="4,0"
                    Background="{StaticResource MediumBgBrush}"
                    ItemsSource="{Binding Source={StaticResource IssueTypeEnum}}"
                    SelectedItem="{Binding PendingIssue.Type}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="WhiteSmoke" Text="{Binding}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                    <ComboBox.ItemContainerStyle>
                        <Style BasedOn="{StaticResource MaterialDesignComboBoxItemStyle}" TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="{StaticResource MediumBgBrush}" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="4" />
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
                <Button
                    Width="120"
                    Margin="3,0"
                    HorizontalAlignment="Right"
                    Command="{Binding CreateIssueCommand}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsIssueCreating}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                    <Setter Property="Content" Value="Creating..." />
                                </DataTrigger>

                                <DataTrigger Binding="{Binding IsIssueCreating}" Value="False">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Setter Property="Content" Value="Create Issue" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>