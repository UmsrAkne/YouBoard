<UserControl
    x:Class="YouBoard.Views.Controls.CustomCheckBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>

        <Button
            Width="24"
            Height="24"
            Padding="0"
            VerticalContentAlignment="Center"
            BorderThickness="3"
            Click="OnButtonClick"
            IsEnabled="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}">

            <Button.Style>
                <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
                    <Setter Property="BorderBrush" Value="DeepSkyBlue" />
                    <Setter Property="Background" Value="SlateBlue" />
                    <Setter Property="Content" Value="" />

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="MediumPurple" />
                        </Trigger>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}, Path=IsCheck, Mode=TwoWay}" Value="True">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <materialDesign:PackIcon Foreground="White" Kind="CheckBold" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="BorderBrush" Value="DimGray" />
                        </Trigger>
                    </Style.Triggers>

                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>